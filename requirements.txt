# Core Django
Django==5.1.7

# Google OAuth2 sign-in
django-allauth[socialaccount]>=0.60.0
asgiref==3.8.1
sqlparse==0.5.3

# PostgreSQL adapter
psycopg2-binary==2.9.11

# Database URL parsing
dj-database-url==2.1.0

# Environment variables
python-decouple==3.8

# Production WSGI server
gunicorn==23.0.0

# Static file serving
whitenoise==6.9.0

# Image processing (QR codes)
Pillow==11.0.0

# QR code generation
qrcode==8.0

# PDF generation (tickets)
reportlab==4.2.5

# Cloud media storage (profile photos on Render)
cloudinary==1.41.0
django-cloudinary-storage==0.3.0

# Machine learning (CI project - Random Forest occupancy prediction)
scikit-learn==1.4.2

# License plate detection (Haar Cascade + contour detection)
# headless = no GUI dependencies, works on Render's Linux containers
opencv-python-headless==4.9.0.80

# OCR — pytesseract is the Render-safe fallback (needs system apt: tesseract-ocr)
# For much better accuracy locally, also run: pip install easyocr
# (easyocr pulls PyTorch ~1.5 GB — too heavy for Render free tier, so excluded here)
